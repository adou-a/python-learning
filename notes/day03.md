1.while 条件：
    print
while True:
    if 条件：  
        continue
    else：
        break


2.for
for i in []
for i in range(数字)
for_ i in range(数字)

3.list
students = [          ]
4.len
表示列表长度
len（students）
5.dictionary
students ={



}
6.split
分割单词，但是字符串中间需要用空格或者逗号或者东西隔开
words = name.split()
7.join
把分开的单词用什么符号结合
result = “-".join(words)
8.isupper()
判断字符是否为大写字母
9.islower()
判断字符是否为小写字母
10.isalpha()
判断字符是不是为字母
11.isdigit()
判断字符是不是为数字
12. not 
if not (s[0].isalpha() and s[1].isalpha()):
not只对第一个有效 
13.False  and  False
答案为False 
所以 and 条件要两边都是True
才能满足
14.布尔表达式
可以定义一个变量为true 或 False
如 number_start = False
if number_start :
意思是if True
15.s[0]
第一个字符
16.s[i]
位置i上的一个字符
17.s[i:]
从位置i开始到最后的一段字符串
18.修改批量变量
鼠标选中变量按F2