让程序做出决定
if  elif else 
三个条件互斥
if走完到elif


模%
偶数表示 
N % 2 == 0 
pythonic
Return True if N % 2 ==0 else False
Return N % 2 ==0
20.match

Match  变量名：
Case ‘ Harry’:
****print(“   “)
Case _:
Print
_表示除了上述条件以外的所有
startwith（‘  ’）  以什么开头,可以是一个字母，也可以是一个单词

if name.startwith(‘he’):
Print(‘   ‘)
endswith(‘  ‘)  以什么结尾，

if answer.endswith(‘jpg’)
Print(‘ ‘)